[Unit]
Description=Periodic drone telemetry home sync

[Timer]
# New footage lands occasionally (after flights), and an incremental run is cheap
# (a discovery pass + only new clips). 6h is responsive enough; away-runs no-op.
OnBootSec=15min
OnUnitActiveSec=6h
Persistent=true

[Install]
WantedBy=timers.target
