# MediaMTX — the van's media hub (R10 in plans/radio-platform-plan.md).
# Radio audio now; Dahua camera proxying for OBS is the intended growth path
# (add `paths:` entries with `source: rtsp://...`).
#
# Read from the deploy checkout by mediamtx.service, so config edits deploy on
# push (the Pi-side hook restarts mediamtx when this file changes). The binary
# is a manual install: /mnt/nvme/mediamtx/mediamtx — a static arm64 build,
# v1.19.2; keys below match that version's schema, and unset keys take that
# version's defaults.
#
# The LAN is trusted (no auth), matching the rest of the app. Only RTSP and
# WebRTC are enabled: VLC/OBS consume RTSP (:8554); the built-in WebRTC page
# (http://<pi>:8889/radio) is the sub-second browser/phone listen path — no
# STUN needed on the offline LAN (host candidates via webrtcIPsFromInterfaces).

logLevel: info

api: false
metrics: false
pprof: false
playback: false
rtmp: false
hls: false
srt: false
moq: false

rtsp: true
rtspAddress: :8554
rtspTransports: [udp, multicast, tcp]

webrtc: true
webrtcAddress: :8889

paths:
  # Live radio audio (Digirig SP1 A+B mix), published by radio-stream.service.
  radio:
    source: publisher
