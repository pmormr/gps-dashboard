[project]
name = "gps-dashboard"
version = "0.1.0"
description = "GPS history browser for a Raspberry Pi van build"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "flask>=3.1.3",
    "requests>=2.32",
    "click>=8.0",
]
