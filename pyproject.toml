[project]
name = "gps-dashboard"
version = "0.1.0"
description = "GPS history browser for a Raspberry Pi van build"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "flask>=3.1.3",
    "requests>=2.32",
    "httpx>=0.27",
    "click>=8.0",
    "paho-mqtt>=2.1.0",
    "bme680>=2.0.0",
]
