# Copy to firmware/secrets.yaml (gitignored) and fill in.
# The mosquitto broker is anonymous (trusted LAN), so no MQTT creds are needed.
wifi_ssid: "van-wifi-ssid"
wifi_password: "van-wifi-password"
ota_password: "pick-an-ota-password"
